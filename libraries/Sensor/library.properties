name=Sensor
version=1.0.0
author=Patrick Dekker
maintainer=Patrick Dekker
sentence=Minimal common setup()/loop() contract for heterogeneous sensor wrappers.
paragraph=Lets BmpSensor, DsSensor, and future sensor wrappers (e.g. a Victron BLE sensor) be driven generically from a Sensor* array in the sketch's setup()/loop(), without forcing their sensor-specific getters into a shared interface.
category=Sensors
architectures=*
includes=Sensor.h
